import { ConvertArrangementToStringPipe } from './convert-arrangement-to-string.pipe';

describe('ConvertArrangementToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertArrangementToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
